using System;

// 7 - Variables and Expressions

/*
namespace CalebCury //a namespace - used for organization
{
    public class Class4 //a class - has members (such as a method)
    {
        static void Main(string[] args) //a method - contain a block of code
        {
            int x = 5;
            int X = 50;
            var y = "hello";

            
            Console.WriteLine(y + " is weird."); //Expression
        }
    }
}
*/
//variable - stores some value (don't start with numbers)
//identifier - what we call the variable
//Expression - evaluates to a value
//operator - it works on operands
//operands - the thing(s) that hte operator works on
